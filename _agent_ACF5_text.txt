\nHere is the structured inventory.\n\n---\n\n## Reference Architecture Audit — Phase 1 (Step 2 Analysis)\n\n---\n\n### 1. MitreAttackWhitelist — Exact Structure\n\nDefined in `f:\\cve-ti-platform\\src\\shared\\mitre\\loader.py`, singleton via `MitreAttackWhitelist.get()`.\n\n**Constructor signature:**\n```python\ndef __init__(\n    self,\n    tactics: frozenset[str],           # e.g. {\"TA0001\", \"TA0002\", ...}\n    techniques: frozenset[str],        # e.g. {\"T1059\", \"T1190\", ...}  (parent only)\n    subtechniques: frozenset[str],     # e.g. {\"T1059.001\", \"T1204.002\", ...}\n    technique_to_tactics: dict[str, list[str]],  # e.g. {\"T1059\": [\"TA0002\"], ...}\n    names: dict[str, str],             # e.g. {\"T1059\": \"Command and Scripting Interpreter\", ...}\n    is_baseline_fallback: bool,\n    source: str,                        # \"stix\" | \"baseline\"\n)\n```\n\n**Derived/composite attributes (computed at construction):**\n```\n.all_techniques = techniques | subtechniques\n```\n\n**Public API:**\n| Method | Returns |\n|---|---|\n| `is_known(tcode)` | `bool` — tactic OR parent OR sub |\n| `is_subtechnique(tcode)` | `bool` |\n| `technique_to_tactics(tcode)` | `list[str]` — TA-codes; sub inherits parent if no direct map |\n| `name_of(tcode)` | `str \\| None` |\n| `refresh()` | reloads singleton in-place |\n| `is_baseline_fallback` | `bool` — True when STIX load failed |\n| `source` | `\"stix\"` or `\"baseline\"` |\n\n**Data source:** MITRE CTI STIX bundle (`enterprise-attack.json`), cached at `{mitre_cache_dir}/enterprise-attack.json` with 7-day TTL. Falls back to hardcoded baseline (~50% matrix, ATT&CK v15 subset) on failure.\n\n---\n\n### 2. attack_validator.py — Whitelist Consumption\n\nDefined in `f:\\cve-ti-platform\\src\\usecases\\step_2_analysis\\rule_based\\attack_validator.py`.\n\n**`__getattr__` dispatch (module-level):**\n\n```\nVALID_TACTICS          → wl.tactics\nVALID_TECHNIQUES       → wl.all_techniques        (parent + sub union)\nVALID_SUBTECHNIQUES    → wl.subtechniques\nanything else          → AttributeError\n```\n\n**`_get_whitelist()` helper:** lazy import of `MitreAttackWhitelist.get()` — avoids triggering the 30-50MB STIX parse at import time. Called by `is_known_ttp()`, `validate_tactic()`, `validate_technique()`, `validate_ttp_list()`.\n\n**Key validation functions:**\n- `is_known_ttp(value)` — returns `\"tactic\"`, `\"parent\"`, `\"sub\"` (with parent-child fallback), or `None`\n- `validate_tactic(value)` / `validate_technique(value)` — boolean wrappers around `is_known_ttp()`\n- `validate_ttp_list(tactics, techniques, subtechniques)` — bulk validation; returns `dict` with `valid_*`, `invalid_*`, `warnings`, `passed`\n- `filter_attack_mapping(...)` — same but strips invalid entries, returns `None` for empty lists\n- `validate_technique_chain_against_phase1(attack_chain, phase1_behaviors)` — evidence-to-TTP consensus validation via keyword/fuzzy match\n\n**No hardcoded frozenset** — all validation routes through the live `MitreAttackWhitelist` singleton.\n\n---\n\n### 3. ExecutionSurface Enum — All Values\n\nDefined in `f:\\cve-ti-platform\\src\\domain\\models\\execution_surface.py`.\n\n```\nExecutionSurface.CLIENT_SIDE   = \"client_side\"\nExecutionSurface.SERVER_SIDE   = \"server_side\"\nExecutionSurface.LOCAL        = \"local\"\nExecutionSurface.MULTI_HOP     = \"multi_hop\"\nExecutionSurface.UNKNOWN       = \"unknown\"\n```\n**5 values total.** Backed by `str, Enum` (JSON-serializable directly).\n\n---\n\n### 4. DeliveryVector Enum — All Values\n\nDefined in `f:\\cve-ti-platform\\src\\domain\\models\\execution_surface.py`.\n\n```\nDeliveryVector.EMAIL_ATTACHMENT    = \"email_attachment\"\nDeliveryVector.EMAIL_LINK          = \"email_link\"\nDeliveryVector.WEB_DOWNLOAD       = \"web_download\"\nDeliveryVector.NETWORK_PROTOCOL    = \"network_protocol\"\nDeliveryVector.PHYSICAL            = \"physical\"\nDeliveryVector.LOCAL_EXECUTION     = \"local_execution\"\nDeliveryVector.UNKNOWN             = \"unknown\"\n```\n**7 values total.** Backed by `str, Enum`.\n\n---\n\n### 5. ExploitVector — Status\n\n**There is no `ExploitVector` enum anywhere in the codebase.** The string literals for exploit vector are produced directly by `classify_exploit_vector()` (see item 7 below) as free-form lowercase strings. They are not enum-backed.\n\n---\n\n### 6. ExploitComplexity — Status\n\n**`ExploitComplexity` enum does not exist in the codebase.** There is no `class ExploitComplexity` anywhere under `src/`. The complexity values are only emitted as raw lowercase strings by `classify_exploit_vector()` (see item 7 below).\n\n---\n\n### 7. exploit_classifier.py — Emitted Literal Strings\n\nDefined in `f:\\cve-ti-platform\\src\\usecases\\step_2_analysis\\rule_based\\exploit_classifier.py`.\n\n**`classify_exploit_vector(cvss_vector) -> dict`**\n\nKeys and literal string values:\n```\n\"exploit_vector\"       : \"remote_network\" | \"adjacent_network\" | \"local\" | \"physical\" | \"unknown\"\n\"remote_exploitable\"   : True | False | None\n\"pre_auth\"             : True | False | None\n\"exploit_complexity\"   : \"low\" | \"high\" | \"unknown\"\n```\n\n**`classify_execution_surface(cvss_vector, description, cwe_ids) -> ExecutionSurface`**\n\nReturns one of the 5 `ExecutionSurface` enum values (see item 3 above). Uses CVSS `AV`, keyword matching against `_CLIENT_SIDE_KEYWORDS` / `_MULTI_HOP_KEYWORDS`, and CWE set membership.\n\n**`classify_delivery_vector(cvss_vector, description, execution_surface) -> DeliveryVector`**\n\nReturns one of the 7 `DeliveryVector` enum values (see item 4 above). Routes based on `AV`, `UI`, and `execution_surface` value.\n\n**Relevant keyword tuples used for classification:**\n\n`_CLIENT_SIDE_KEYWORDS` (43 entries): `\"office\"`, `\"word\"`, `\"excel\"`, `\"powerpoint\"`, `\"outlook\"`, `\"onenote\"`, `\"mshtml\"`, `\"activex\"`, `\"ole\"`, `\"macro\"`, `\"vba\"`, `\"pdf reader\"`, `\"pdf parser\"`, `\"adobe reader\"`, `\"acrobat\"`, `\"browser\"`, `\"chrome\"`, `\"firefox\"`, `\"edge\"`, `\"safari\"`, `\"blink\"`, `\"media player\"`, `\"media parser\"`, `\"media foundation\"`, `\"java applet\"`, `\"flash player\"`, `\"silverlight\"`\n\n`_MULTI_HOP_KEYWORDS` (18 entries): `\"supply chain\"`, `\"software supply chain\"`, `\"build pipeline\"`, `\"ci/cd\"`, `\"package manager\"`, `\"dependency\"`, `\"typosquatting\"`, `\"container escape\"`, `\"runc\"`, `\"docker escape\"`, `\"kubernetes\"`, `\"hypervisor\"`, `\"vm escape\"`, `\"ci pipeline\"`, `\"backdoor\"`, `\"trojanized\"`, `\"compromised build\"`\n\n---\n\n### Summary Table\n\n| Symbol | Type | Values | Defined in |\n|---|---|---|---|\n| `MitreAttackWhitelist` | class (singleton) | attrs: `tactics`, `techniques`, `subtechniques`, `all_techniques`, `_technique_to_tactics`, `_names`, `is_baseline_fallback`, `source` | `loader.py` |\n| `ExecutionSurface` | `str, Enum` | `client_side`, `server_side`, `local`, `multi_hop`, `unknown` | `execution_surface.py` |\n| `DeliveryVector` | `str, Enum` | `email_attachment`, `email_link`, `web_download`, `network_protocol`, `physical`, `local_execution`, `unknown` | `execution_surface.py` |\n| `ExploitVector` | **does not exist** | — | — |\n| `ExploitComplexity` | **does not exist** | — | — |\n| `exploit_vector` output | raw `str` | `remote_network`, `adjacent_network`, `local`, `physical`, `unknown` | `exploit_classifier.py` |\n| `exploit_complexity` output | raw `str` | `low`, `high`, `unknown` | `exploit_classifier.py` |