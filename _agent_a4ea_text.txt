\nNow I have everything. Here is the complete findings report:\n\n---\n\n## Audit Report: Execution Surface Data Sources\n\n### 1. MITRE STIX — `x_mitre_platforms` Field\n\n**Verdict: CONFIRMED. Field exists and is usable.**\n\n- Location: `f:\\cve-ti-platform\\.cache\\mitre_attack\\enterprise-attack.json`\n- Every `attack-pattern` STIX object carries it as a top-level key.\n\n```\nAll unique x_mitre_platforms values (11 total):\n  Containers, ESXi, IaaS, Identity Provider, Linux, Network Devices,\n  Office Suite, PRE, SaaS, Windows, macOS\n```\n\nThe `x_mitre_is_subtechnique` boolean is also present on every attack-pattern object.\n\n**Proxy mapping for `execution_surface`:**\n\n| `x_mitre_platforms` value | `execution_surface` candidate |\n|---|---|\n| `Windows`, `Linux`, `macOS` | `client_side` or `local` (endpoint) |\n| `Containers` | `multi_hop` (container escape context) |\n| `IaaS`, `SaaS`, `Office Suite` | `server_side` (cloud/ SaaS) |\n| `Identity Provider` | `server_side` (auth service) |\n| `Network Devices`, `ESXi` | `local` or `server_side` (infra) |\n| Mixed (e.g., `[\"IaaS\",\"Linux\",\"macOS\",\"SaaS\",\"Windows\"]`) | multi-hop supply chain |\n\nThe loader (`src/shared/mitre/loader.py`) currently **parses `x_mitre_platforms` from STIX but discards it entirely** — it is never stored on `MitreAttackWhitelist`.\n\n---\n\n### 2. CAPEC Schema\n\n**Verdict: Schema fields confirmed, but no local loader or data file exists.**\n\nWeb search confirms the CAPEC XML schema (current: v3.9) defines these fields that map to `execution_surface`:\n- **`prerequisites`** — conditions that must exist for attack viability\n- **`Resources_Required`** — tools/access/skills needed\n- **`Attack_Surface`** — entry point type (network/local/physical)\n- **`Impact`** — technical impact (execute code, read data, DoS)\n- **`Likelihood`**, **`Severity`** — scoring\n\nNo CAPEC data file (XML, JSON, or Python loader) exists anywhere under `f:\\cve-ti-platform`. The URL `https://capec.mitre.org/data/xsd/capec_ap_schema_v3.9.xsd` returned HTTP 404 (likely requires the full path with schema subdirectory). The zip file URL also has no public JSON mirror.\n\n---\n\n### 3. MitreAttackWhitelist — Current Attribute Model\n\n**Verdict: Platforms are NOT stored. The loader must be extended.**\n\nCurrent `MitreAttackWhitelist.__dict__` keys:\n\n| Attribute | Type | Content |\n|---|---|---|\n| `tactics` | `frozenset[str]` | 15 TA-codes |\n| `techniques` | `frozenset[str]` | 222 parent T-codes |\n| `subtechniques` | `frozenset[str]` | 475 dotted T-codes |\n| `all_techniques` | `frozenset[str]` | union of above (697 total) |\n| `_technique_to_tactics` | `dict[str, list[str]]` | T-code → [TA-codes], 697 keys |\n| `_names` | `dict[str, str]` | T-code → name, 712 entries |\n| `is_baseline_fallback` | `bool` | True if STIX load failed |\n| `source` | `str` | `\"stix\"` or `\"baseline\"` |\n\n**Missing**: `_platforms: dict[str, list[str]]` (T-code → platform list) is not extracted or stored.\n\nTo add it, `_parse_stix_file()` at line 416 would need:\n```python\nplatforms: dict[str, list[str]] = {}\n# ...\nplatforms[tcode] = obj.get(\"x_mitre_platforms\", []) or []\n```\n\n---\n\n### 4. Phase 1 System Prompt — Current Schema\n\n**File**: `f:\\cve-ti-platform\\src\\usecases\\step_2_analysis\\prompts\\analyze_behavior_phase1.system.txt`\n\nThe 3 canonical fields already exist in the Phase 1 schema (lines 29-31):\n```json\n\"execution_surface\": str | null,   // \"client_side\" | \"server_side\" | \"local\" | \"multi_hop\" | \"unknown\"\n\"delivery_vector\": str | null,     // \"email_attachment\" | \"email_link\" | \"web_download\" | ...\n\"user_interaction_required\": bool | null,\n```\n\nThe prompt defines `execution_surface` by description (not by external data) and asks the LLM to derive it from CVE description text alone. The anti-bias rule at lines 97-99 explicitly warns against using CVSS `AV:N` as sole trigger.\n\n---\n\n### 5. Existing Infrastructure\n\n| File | Status | Relevant fields |\n|---|---|---|\n| `src/shared/mitre/loader.py` | EXISTS | STIX already loaded; `x_mitre_platforms` present but unused |\n| `src/shared/mitre/fetch_stix.py` | EXISTS | CLI to refresh STIX cache |\n| `src/shared/parsers/cvss_parser.py` | EXISTS | `parse_cvss_vector()` returns `attack_vector` (AV:N/L/A/P) and `privileges_required` (PR:N/L/H) |\n| `src/shared/parsers/cpe_parser.py` | EXISTS | No `execution_surface` mapping |\n| `src/shared/parsers/reference_parser.py` | EXISTS | No `execution_surface` mapping |\n| `src/shared/cve_loader.py` | DOES NOT EXIST | — |\n| `src/shared/parsers/capec_loader.py` | DOES NOT EXIST | — |\n| `resources/*.yaml` | DOES NOT EXIST | — |\n| `data/*.yaml` | DOES NOT EXIST | — |\n\n---\n\n### 6. CVSS as Execution Surface Proxy\n\n`cvss_parser.py` provides `is_network_reachable()`, `is_local_only()`, `is_pre_auth()` — these are already in the codebase and usable as rule-based anchors for `execution_surface`:\n\n| CVSS signal | Likely `execution_surface` |\n|---|---|\n| `AV:N` + no client-app keywords in description | `server_side` |\n| `AV:N` + document/browser keywords | `client_side` (anti-bias override) |\n| `AV:L` or `AV:P` | `local` |\n| CWE-829 + \"supply chain\"/\"container escape\" | `multi_hop` |\n| `PR:N` | `pre_auth: true`, supports `server_side` |\n| `UI:R` (Required) | `user_interaction_required: true` |\n\n---\n\n### Summary of Concrete Evidence\n\n**The 3 canonical Phase 1 fields (`execution_surface`, `delivery_vector`, `user_interaction_required`) have two available anchor sources that are already in the codebase but unused:**\n\n1. **STIX `x_mitre_platforms`** — 11 platform values in `f:\\cve-ti-platform\\.cache\\mitre_attack\\enterprise-attack.json`, parsed by `src/shared/mitre/loader.py` but dropped before storage. Adding `platforms: dict[str, list[str]]` to `MitreAttackWhitelist` and passing it through `_parse_stix_file()` requires ~5 lines of code.\n\n2. **CVSS metrics** — `src/shared/parsers/cvss_parser.py` already parses `AV`, `PR`, `UI`. A rule-based module (or Phase 1 system prompt update) can use `is_network_reachable()`, `is_local_only()`, `is_pre_auth()` as deterministic anchors instead of relying entirely on the LLM to infer from description text.\n\n3. **No external fetch needed** — CAPEC is the only signal that would require a new data source, and it has no local cache or working public URL.\n\nThe path forward is to extend `MitreAttackWhitelist` to store `platforms`, then wire that data into the rule-based validator (`src/usecases/step_2_analysis/rule_based/`) or Phase 1 service as a deterministic input alongside the LLM's inference.